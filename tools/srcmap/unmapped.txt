Wall: BOTS is in the tree but VPX never writes it
Wall: COLL is in the tree but VPX never writes it
Wall: DILI is in the tree but VPX never writes it
Wall: DLBE is in the tree but VPX never writes it
Wall: DLTO is in the tree but VPX never writes it
Wall: DTEX is in the tree but VPX never writes it
Wall: FRIC is in the tree but VPX never writes it
Wall: IMGF is in the tree but VPX never writes it
Wall: IMGS is in the tree but VPX never writes it
Wall: MATL is in the tree but VPX never writes it
Wall: MATP is in the tree but VPX never writes it
Wall: MATR is in the tree but VPX never writes it
Wall: PMAT is in the tree but VPX never writes it
Wall: REFL is in the tree but VPX never writes it
Wall: SCAT is in the tree but VPX never writes it
Wall: SIVI is in the tree but VPX never writes it
Wall: SLAN is in the tree but VPX never writes it
Wall: SLFO is in the tree but VPX never writes it
Wall: TBVI is in the tree but VPX never writes it
Wall: TMRN is in the tree but VPX never writes it
Rubber: RADB is in the tree but VPX never writes it
Flasher: IDMD is in the tree but VPX never writes it
Gate: IMGB is in the tree but VPX never writes it
Gate: IMGF is in the tree but VPX never writes it
Primitive: M3DX (WriteRaw) the source tree has no field for it
Primitive: M3CX (WriteRaw) no JSON representation for Vec<u8>
Primitive: M3DI (WriteRaw) the source tree has no field for it
Primitive: M3CI (WriteRaw) no JSON representation for Vec<u8>
Primitive: M3DI (WriteRaw) the source tree has no field for it
Primitive: M3CI (WriteRaw) no JSON representation for Vec<u8>
Primitive: M3AY (WriteInt) the source tree has no field for it
Primitive: M3AX (WriteRaw) the source tree has no field for it
Primitive: BMAX is in the tree but VPX never writes it
Primitive: BMIN is in the tree but VPX never writes it
Primitive: DILI is in the tree but VPX never writes it
Primitive: M3CI is in the tree but VPX never writes it
Primitive: M3CX is in the tree but VPX never writes it
HitTarget: DILI is in the tree but VPX never writes it
DragPoint: DPNT (None) the source tree has no field for it
DragPoint: LANR is in the tree but VPX never writes it
DragPoint: LAYR is in the tree but VPX never writes it
GameData: MATE (WriteRaw) the source tree has no field for it
GameData: PHMA (WriteRaw) the source tree has no field for it
GameData: CCUS (WriteRaw) the source tree has no field for it
GameData: CODE (WriteScript) no JSON representation for StringWithEncoding
GameData: ARAC is in the tree but VPX never writes it
GameData: AVSY is in the tree but VPX never writes it
GameData: BREF is in the tree but VPX never writes it
GameData: BRST is in the tree but VPX never writes it
GameData: BTRA is in the tree but VPX never writes it
GameData: BTST is in the tree but VPX never writes it
GameData: CLMO is in the tree but VPX never writes it
GameData: CODE is in the tree but VPX never writes it
GameData: MAXS is in the tree but VPX never writes it
GameData: MPDF is in the tree but VPX never writes it
GameData: MPGC is in the tree but VPX never writes it
GameData: OGAC is in the tree but VPX never writes it
GameData: OGDN is in the tree but VPX never writes it
GameData: OGST is in the tree but VPX never writes it
GameData: REOP is in the tree but VPX never writes it
GameData: SECB is in the tree but VPX never writes it
GameData: TBLH is in the tree but VPX never writes it
GameData: UAAL is in the tree but VPX never writes it
GameData: UFXA is in the tree but VPX never writes it
